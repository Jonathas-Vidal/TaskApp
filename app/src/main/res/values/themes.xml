<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskCreateAndManageYourTasks" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/color_default</item>
        <item name="android:windowBackground">@color/color_default</item>
        <item name="android:statusBarColor">@color/color_default</item>
    </style>

    <style name="Theme.TaskCreateAndManageYourTasks" parent="Base.Theme.TaskCreateAndManageYourTasks" />

    <!--Edit text theme -->
    <style name="EditTextTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:background">@drawable/bg_edit_text</item>
        <item name="android:padding">14dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColorHint">#8F8D8D</item>
    </style>

    <!--Button theme -->
    <style name="ButtonTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:background">@drawable/bg_button</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/color_default</item>
        <item name="backgroundTint">@null</item>
    </style>
    
    <style name="BottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/bottomSheetStyleWrapper</item>
    </style>

    <style name="bottomSheetStyleWrapper" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>